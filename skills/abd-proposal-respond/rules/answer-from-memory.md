# Answer from Memory

**DO** — Use `search_memory "<query>"` to retrieve relevant chunks before drafting each answer. Cite sources (document, section, slide/page).

- **Example (wrong):** Answering from general knowledge when proposal content exists in memory.
- **Example (correct):** Run `search_memory "Agile adoption approach"`; inject top chunks; draft answer citing retrieved content.

**DO NOT** — Answer questions without checking memory when proposal material has been indexed.
